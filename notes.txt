This is a Hugo static site (https://gohugo.io/) using the Archie theme (https://github.com/athul/archie).

First checkout (theme is a git submodule):
- git submodule update --init --recursive

To work:
- hugo server

To publish:

- hugo --cleanDestinationDir
- ghp-import public
- git push origin gh-pages
